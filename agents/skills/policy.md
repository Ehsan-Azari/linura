# Policy task guide

Policy decides whether a validated plan may proceed.

- Agent identity never implies elevated authority.
- Security-sensitive, destructive, exposure-changing, and credential-affecting actions require explicit policy treatment.
- Approval UI must describe actual typed effects, not only the agent's prose summary.
- Add deny-path tests before allow-path convenience.
