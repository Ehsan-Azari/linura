# Linura bootstrap

This directory owns the future minimal installer/first-boot/recovery product boundary. It must preserve offline installation/recovery paths and snapshot/rollback gates. See `docs/bootstrap-recovery.md` and `docs/first-boot.md`.
