# Hardware evidence

Hardware support is evidence-based. `fixtures/` contains sanitized structural test fixtures and `support-matrix.json` records the strongest evidence tier available for each domain.

Fixtures are not physical support certification. See `docs/hardware-validation.md` before adding hardware observations.
