# Linura Agent

Planned conversational intent client for Linura.

Natural language is converted to a structured `IntentProposal`. Linura Agent never receives privileged executor access and cannot bypass Linura Control, policy, approval, planning, verification or provenance requirements.
