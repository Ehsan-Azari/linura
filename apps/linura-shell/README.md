# Linura Shell

Planned cohesive desktop shell providing launcher, quick settings, notifications and status surfaces over the same versioned Linura protocol used by the CLI, Control Center and SDK.

Linura Shell contains no provider-specific system mutation logic. Shell failure must never prevent deterministic control or recovery.
