#![forbid(unsafe_code)]

fn main() {
    println!("Linura first boot");
    println!("What do you want this computer to become?");
    println!("\nThe production application will convert conversation into a reviewable IntentProposal; it will never execute model-generated shell text.");
}
