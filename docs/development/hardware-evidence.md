# Recording hardware evidence

When adding physical hardware evidence, record only the minimum information required to reproduce/support the behavior: component class, vendor/model family, kernel/provider versions, profile ID, observed behavior, test run reference, and evidence tier.

Do not commit device serials, MAC addresses, personal hostnames, account data, private IP topology, or arbitrary diagnostic dumps. Sanitize first.
