# v0.4.0 release review and terminal publication record

This record distinguishes implementation qualification, release authorization, publication and independent verification for the bounded Experimental v0.4.0 release. Pending evidence was never treated as terminal proof; all terminal identifiers below correspond to completed repository-controlled events.

## Final implementation closure

The v0.4 implementation evolved through proof-discovered blockers that were fixed rather than waived:

- **PR #54** established the durable reviewed-authority transaction/recovery foundation; final implementation head `e92786f2c4e545bd289692f6199060da9e3a0248`, protected-main merge `5e9e9bff03dcd24bca0f3bb3c735e37b49aacdd7`.
- **PR #58** closed recovery-reserve rollback reconciliation and migration-ledger validation gaps; exact head `1a3a9b343d3830f384128fe6e42a2a0c2a394898`, merge `4f6ad21aa45cbce783bdd6c6ef7e3cc347dca18b`.
- **PR #61** increased physically allocated recovery reserve and made both v0.4 fault suites mandatory Trusted Release Proof dependencies; exact head `ee1e82608f9693f47eb72ea88cf641154e462232`, merge `c92c1e161eb4b60b39170c3ae24253ae81c453c8`.
- **PR #62** closed the final true-ENOSPC reopen/rollback class with pre-open terminal headroom, caller-limit preservation, immutable V1 plus authenticated V2 migration, literal migration pins and commit-ordered reserve reconciliation; exact head `8ebf8e3c3b861f222c228c379ea21478e296b205`, merge `072ca08c92a8e146aab49ded8a7f94bf646394d6`.

The final implementation lineage for publication therefore ends at the PR #62 protected-main merge rather than any obsolete pre-PR-#62 release authorization.

## Final implementation qualification and adversarial review

Exact implementation head `8ebf8e3c3b861f222c228c379ea21478e296b205` passed:

- CI `33917843348`;
- Security `33917843428`;
- CodeQL `33917843396`;
- Disposable VM acceptance `33917843410`;
- Control1 plan-preview VM acceptance `33917843590`;
- genuine loop-mounted ext4 ENOSPC recovery `33917843426`;
- SQLite/WAL durability fault qualification `33917843352`.

The fault qualification includes deterministic write denial, process `SIGKILL` after acknowledged durable handoff, abrupt QEMU power loss, same-image restart, sticky `Indeterminate`, WAL checkpoint and reopen. The ENOSPC suite includes true filesystem exhaustion, new-process terminal-recovery open, Prepared retirement, reopen, unmount/remount and terminal integrity validation.

All actionable inline findings were resolved only after corrected exact-head qualification. Fresh adversarial review covered migration compatibility/immutability, sparse-opener bounds, custom limits, rollback ordering, post-commit reserve reconciliation, concurrency, retained-input bounds, restart behavior, authority boundaries and accidental execution authority, with no remaining actionable finding.

The review requirement is exact-source and substantive; it is not tied to any particular review vendor.

## Frozen release contract and reviewed source

`docs/releases/v0.4.0.md` is the immutable claim contract for v0.4.0. The final reviewed release source was:

- reviewed source `2f8bb4c99ad2004b7edfcb07115c90a8c709d510`;
- reviewed tree `141545fe863413db506960c7b4e2b18dac9ea62a`.

The release-preparation tree preserved the workspace/package version, coherent lockfile, CHANGELOG/README release material, qualification records and automation without widening the claim to a supported executor or external mutation.

## Release authorization contract

Publication authority was represented by the single-parent metadata-only commit `0ff6626a7bc5cb0288b8da9614c4094df2db35e5`. Its Git tree is identical to the reviewed source tree and its `Reviewed-Source` / `Reviewed-Tree` trailers bind the authorization to that exact reviewed material.

This avoids self-referential release evidence: tracked files do not have to change merely to encode the reviewed tree. The release intent validator rechecked the source/tree relationship before proof dispatch and inside Trusted Release Proof.

## Protected proof-first/tag-last publication

The protected release lifecycle completed in this order:

1. fresh protected-main release-authorization checks;
2. **Trusted Release Proof `33920772289` — success**, including authoritative-observation VM, Control1 plan-preview VM, SQLite/WAL durability-fault and genuine ext4 ENOSPC qualification on the exact release authorization;
3. isolated exact-source build, independent reproduction and sealed release evidence;
4. **Release Promotion `33921655450` — success**, consuming the sealed proven bytes without rebuild;
5. **release publication `33921670086` — success**, with proof-first/tag-last publication;
6. immutable GitHub Release **`v0.4.0`**, release id `383026672`, published `2026-09-04T21:35:50Z`;
7. **Independent Release Verification `33921833189` — success** from a fresh verifier path.

The canonical compact terminal evidence record is [`v0.4.0-publication.md`](v0.4.0-publication.md).

## Post-publication closure

Post-publication repository bookkeeping is now closed on the v0.5 development lineage:

- `contracts/roadmap.toml` records `current_release = "v0.4.0"` and `next_release = "v0.5.0"`;
- the canonical roadmap marks v0.4 released and v0.5 planned;
- README reports v0.4 as released rather than publication-pending;
- the v0.4 milestone and qualification dossier point to terminal publication/verification evidence;
- the immutable v0.4 tag, assets and frozen release contract remain unchanged.

## Boundary retained after publication

v0.4.0 remains Experimental and deliberately has:

- `executor_state = "none"`;
- `managed_mutation_support = "none"`;
- `complete_lifecycle = false`;
- no public apply/execute/mutate surface;
- no supported distribution, machine class or hardware profile;
- no privileged executor or Polkit authorization in the released product boundary.

Durable transaction state, policy `Allow`, approval evidence, reviewed-plan state, transaction/generation identity, `Prepared`, `Indeterminate`, audit records and verification metadata do not become execution credentials merely because the release is published. v0.5 may qualify one isolated narrow executor/verifier component; the first supported managed external effect remains gated on v0.6 complete-lifecycle qualification.
