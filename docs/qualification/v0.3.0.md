# v0.3.0 qualification — policy, authorization, approval, and plan review

This document remains the v0.3.0 **qualification specification** for the implemented **Experimental** authority-review milestone. Implementation qualification is complete enough to freeze the release contract; proof that the capability is released exists only after the exact release-intent source completes Trusted Release Proof, immutable tag-last publication, and independent published-release verification.

## Claim boundary

v0.3.0 may claim only that Linura can take its canonical evidence-bound non-executable reconciliation plan through deterministic trusted risk classification, policy and approval review while preserving authenticated-principal binding and the non-mutation boundary.

It may not claim durable prepare/recovery, executor integration, managed external mutation, post-effect verification, complete lifecycle execution, complete risk coverage for future domains, a supported platform profile or production readiness.

## Required evidence groups

### 1. Canonical plan lineage

Prove that policy review consumes a subject derived from `linura-planner::ReconciliationPlan` and does not revive provider-owned planning or the removed bootstrap `ActionPlan` path.

Required checks:

- no public/in-repo `ActionRequest`, `PlanResponse`, generic `Provider::plan`, `ControlPlane::apply` or `MutationRuntime` authority path remains;
- policy review projection is created from the canonical reconciliation plan plus authenticated principal identity;
- trusted risk classification is owned by Control and consumes canonical typed plan material rather than client/model/provider risk claims;
- providers remain bounded observation mechanisms and do not own policy/approval orchestration;
- D-Bus remains authentication/wire adaptation only.

### 2. Deterministic risk classification and policy outcomes

The planner's `prospective_risk` is treated as a lower bound. Required positive/negative cases:

- canonical systemd `active_state` change is elevated to `SecuritySensitive` by the baseline trusted risk rule;
- canonical security-sensitive work reaches administrator approval;
- a canonical plan classified `Destructive` by a trusted test rule reaches destructive-action approval without claiming a supported destructive mutation rule;
- unmatched/unclassified canonical mutation becomes `blocked`;
- risk downgrade below the planner floor is rejected and becomes `blocked`;
- overlapping rules choose the highest risk deterministically;
- the same normalized plan and risk-policy revision/rules produce the same classification;
- `allow` for a permitted subject;
- explicit `deny`;
- `require-approval` for interactive-user class;
- `require-approval` for administrator class;
- `require-approval` for destructive-action class;
- `blocked` for a plan containing blockers/unknown required state;
- unknown/unsupported policy input fails closed rather than defaulting to allow.

### 3. Exact review binding

Every policy evaluation and approval check must prove binding to:

- authenticated principal;
- request ID;
- plan ID;
- material canonical planned changes/findings;
- authoritative evidence ID;
- provider/resource/capability identity;
- semantic provenance;
- planner risk floor and classified prospective risk;
- risk-policy revision and matched rule identities;
- policy ID/revision;
- approval requirement/evidence when applicable.

Negative cases must reject substitution of each materially security-relevant field. A plan ID by itself is not sufficient authority evidence.

### 4. Approval lifecycle

When approval is required, evidence must prove:

- approver identity is authenticated rather than client-asserted;
- the approver satisfies the required class/constraint;
- an agent cannot approve its own mutation proposal;
- approval for one principal cannot be stolen by another principal;
- approval for one plan/evidence/policy or risk-policy revision cannot authorize another;
- issuance and expiry are explicit;
- expired approval is rejected;
- revoked approval is rejected;
- replay after a material review-binding change is rejected;
- missing or malformed approval fails closed.

### 5. Explainability and provenance

A reviewed result must retain enough typed information to explain:

- who/what requested the change (`Actor` provenance);
- which authenticated principal owns the review;
- why the desired state exists (semantic origin);
- what material state changes/findings were reviewed;
- which authoritative evidence was used;
- which trusted risk-policy revision/rules classified the plan and what risk resulted;
- which policy snapshot produced the decision;
- whether approval was required and what evidence satisfied it.

Model-generated prose is not authoritative policy or risk-classification evidence.

### 6. Resource and hostile-input bounds

Every new public request, retained review and approval representation must define and test bounded:

- identifier/string sizes;
- number of findings/changes/origins;
- approval metadata size;
- retained entry count;
- per-entry and aggregate retention bytes;
- malformed/duplicate fields;
- deterministic eviction behavior if process-local retention is used.

Risk classification and policy evaluation must be deterministic, bounded and free of unbounded polling, retries or background work.

### 7. Non-execution proof

Unit/integration tests and exact-source VM acceptance must prove all of the following even after a plan receives an `allow` result or valid approval:

- reviewed plan remains explicitly non-executable;
- no executor handle is returned;
- no Polkit authorization is requested;
- no durable prepare record is created;
- no shell/generic privileged command path is invoked;
- native Linux state remains unchanged;
- v0.2 plan-preview regression cases remain green.

The strongest system assertion is independent re-observation/native inspection of the controlled fixture before and after review/approval.

### 8. Security/threat-model evidence

Tests/review must cover at minimum:

- approval replay;
- stale-plan/stale-evidence approval;
- policy substitution/downgrade;
- risk-policy substitution/downgrade;
- client/model/provider attempts to assert a weaker risk;
- unclassified mutation defaulting to a weaker class;
- cross-principal approval theft;
- agent self-approval;
- approver-class escalation;
- confused deputy behavior;
- forged client actor/principal identity;
- TOCTOU between plan review and any later milestone's prepare boundary;
- malicious retrieved/model context attempting to influence authority.

`docs/threat-model.md` and ADR 0018 are normative review inputs.

## Expected system qualification

The v0.3 exact-source disposable VM scenario should extend the v0.2 Control1 plan-preview fixture rather than create a separate provider-owned planning path. At minimum it must:

1. authenticate a local caller;
2. observe the controlled native resource authoritatively;
3. produce a deterministic change-proposed plan preview;
4. classify the canonical plan with the trusted risk policy and retain classification provenance;
5. obtain the expected deterministic policy result;
6. exercise the required-approval boundary where applicable: the real daemon/CLI VM path must prove the required approval class, while authenticated approval-evidence issuance/expiry/revocation remains qualified by exact-source deterministic Control tests until a trusted human interaction adapter exists; service D-Bus credentials must never be promoted into human/admin approval authority;
7. retrieve/explain the reviewed result;
8. test at least one binding mismatch/replay rejection;
9. test an unclassified/downgrade risk rejection in deterministic authority tests;
10. prove every public result remains non-executable;
11. independently re-observe/native-inspect the resource and prove it did not change.

The existing authoritative-observation and plan-preview VM scenarios remain mandatory regressions.

## Implementation qualification already established

The mutable implementation closed through two protected changes:

- [PR #45](https://github.com/linura-org/linura/pull/45) established the canonical review-only authority foundation, Control-only policy orchestration, principal/actor separation, trusted fail-closed risk classification, authority anti-drift checks, ADR 0018, and the v0.3 threat/qualification boundary.
- [PR #49](https://github.com/linura-org/linura/pull/49) completed the exact-bound approval lifecycle and public review path. Its final exact head `a68d622ef0ebc9c881f99ff4dd2cac14f6eff159` passed CI, Security, CodeQL, authoritative-observation VM, and Control1 plan-preview/review VM qualification with all review threads resolved and a fresh final Codex no-findings review before protected merge. The protected-main implementation merge is `e7ae91085e200a90867eadd19f9f4dac39f0d769`.

The final implementation review specifically closed defects involving forged policy evaluations, caller-selected revocation/time, wall-clock rollback, retained-review allocation bounds, inactive-record reclamation, approval-class downgrades, blocked/allow wire inconsistency, proposed-change/read-only inconsistency, and missing CLI semantic provenance. These are permanent regression concerns, not waived release exceptions.

Real VM qualification exercises daemon/CLI plan review and required approval-class projection but deliberately does not forge a trusted-human approval over D-Bus. Approval issuance/expiry/revocation/authenticated-approver semantics remain qualified by deterministic Control-owned tests until a future trusted human/admin adapter exists.

## Concrete claim-to-evidence map

This map distinguishes **implementation evidence already established** from **publication evidence that is intentionally still pending**. Source/test paths are reproducible from the protected implementation merge `e7ae91085e200a90867eadd19f9f4dac39f0d769`. Historical workflow links are immutable evidence for PR #49's final exact implementation head `a68d622ef0ebc9c881f99ff4dd2cac14f6eff159`; they do not substitute for the later exact release-intent proof.

| Claim / invariant | Reproducible implementation evidence | Protected/system evidence | State |
| --- | --- | --- | --- |
| Canonical `ReconciliationPlan` lineage and Control-only policy authority | `crates/linura-control/src/policy_review.rs`: `canonical_plan_projects_exact_review_material`, `canonical_systemd_change_requires_administrator_approval`; `tools/check_authority_foundation.py`; `tests/tooling/test_authority_foundation.py::test_repository_authority_foundation_is_valid` | [PR #49 CI run 33640795667](https://github.com/linura-org/linura/actions/runs/33640795667) plus protected layering/authority checks in `cargo xtask check` | established for implementation |
| Planner risk is a floor; unknown/downgraded risk fails closed; overlap chooses strongest rule | `crates/linura-control/src/risk_classification.rs`: `baseline_systemd_active_state_is_security_sensitive`, `unmatched_change_shape_fails_closed`, `lower_rule_cannot_downgrade_planner_floor`, `canonical_plan_can_be_classified_destructive_by_trusted_rule`, `overlapping_rules_choose_the_highest_risk_deterministically`; `policy_review.rs`: `unclassified_canonical_mutation_fails_closed`, `attempted_risk_downgrade_blocks_before_policy_allow` | [PR #49 CI run 33640795667](https://github.com/linura-org/linura/actions/runs/33640795667); [Control1 VM run 33640796090](https://github.com/linura-org/linura/actions/runs/33640796090) proves the canonical systemd route is reviewed as `security-sensitive` / administrator approval | established for implementation |
| Exact review binding; caller-forged policy evaluation cannot weaken approval | `crates/linura-control/src/approval_review.rs`: `exact_trusted_review_is_the_approval_binding`, `forged_policy_evaluation_cannot_weaken_trusted_review`, `weak_approver_cannot_satisfy_security_sensitive_requirement`, `blocked_and_allowed_reviews_cannot_be_minted_as_approval` | [PR #49 CI run 33640795667](https://github.com/linura-org/linura/actions/runs/33640795667); [Security run 33640795892](https://github.com/linura-org/linura/actions/runs/33640795892); [CodeQL run 33640795711](https://github.com/linura-org/linura/actions/runs/33640795711) | established for implementation |
| Approval expiry, authoritative revocation, Control-owned time, rollback resistance, idempotency and replay prevention | `approval_review.rs`: `process_local_issue_is_idempotent_and_conflicts_fail_closed`, `revocation_is_authoritative_even_when_evidence_was_cloned`, `unqualified_revoker_is_rejected`, `expired_records_are_reclaimed_and_request_replay_stays_blocked`, `revoked_records_are_reclaimed_without_reopening_request_id`, `public_authority_api_uses_control_owned_time`, `authority_clock_rollback_cannot_revive_expired_evidence` | [PR #49 CI run 33640795667](https://github.com/linura-org/linura/actions/runs/33640795667) plus final exact-head Codex authority/security review on PR #49 | established for implementation |
| Retention and hostile-input bounds are applied before retained allocation and remain bounded | `approval_review.rs`: `oversized_review_is_rejected_before_retained_clone`, `expired_records_are_reclaimed_and_request_replay_stays_blocked`, `tombstone_retention_is_bounded_and_deterministically_pruned`, `retained_bytes_are_bounded_and_idempotent_retry_does_not_grow_store`; transport bounds in `crates/linura-dbus/src/planning.rs` | [PR #49 CI run 33640795667](https://github.com/linura-org/linura/actions/runs/33640795667) | established for implementation |
| D-Bus/SDK/CLI preserve reviewed risk, approval strength and semantic provenance but do not own authority | `crates/linura-dbus/src/planning.rs` enforces reviewed-risk/approval-class consistency, protected-`allow` and status/risk invariants; `tests/acceptance/008-control1-plan-preview.json` exercises plan review/explanation and semantic reason/origins | [Control1 VM run 33640796090](https://github.com/linura-org/linura/actions/runs/33640796090) | established for implementation |
| Review remains non-executable and native Linux state is unchanged | public protocol/SDK/CLI review projections set `execution_authorized=false`; `tests/acceptance/008-control1-plan-preview.json` independently inspects the native fixture before/after review; v0.2 observation regression remains mandatory | [Disposable authoritative-observation VM run 33640795781](https://github.com/linura-org/linura/actions/runs/33640795781) and [Control1 VM run 33640796090](https://github.com/linura-org/linura/actions/runs/33640796090) | established for implementation |
| Threat model: models remain untrusted proposers; D-Bus caller/UID 0 is not human approval; no prepare/executor/Polkit authority | `docs/threat-model.md`, `docs/adr/0018-canonical-plan-review-authority.md`, Control approval tests above, authority-foundation checker | [Security run 33640795892](https://github.com/linura-org/linura/actions/runs/33640795892), [CodeQL run 33640795711](https://github.com/linura-org/linura/actions/runs/33640795711), and final no-findings authority review on PR #49 | established for implementation |
| Release authorization cannot modify the reviewed release-preparation tree | `tools/release_contract.py::validate_release_intent`; `tests/tooling/test_release_contract.py` positive exact-tree case plus changed-tree and forged-trailer rejection; `.github/workflows/release-proof-dispatch.yml` and `.github/workflows/trusted-release-proof.yml` independently enforce the same invariant | PR #51 exact-final-head CI/review is required before merge; later protected-main release-intent CI/Security/CodeQL and Trusted Release Proof must execute against the exact metadata-only authorization commit | implementation of the control is under release-prep review; publication evidence pending |
| Sealed bytes, reproducibility, immutable tag-last publication and independent published-release verification | `.github/workflows/trusted-release-proof.yml`, `.github/workflows/reusable-release-build.yml`, `.github/workflows/release-promotion.yml`, `.github/workflows/release.yml`, release verification workflows/tools | No v0.3.0 proof/promotion/tag/release/independent-verification run exists yet by design | **pending; required for publication** |

The first eight rows are evidence that the bounded v0.3 implementation exists and is review-only. The final two rows are release-process claims: they remain open until the exact final PR #51 head is accepted and the later protected release-intent/proof/promotion/publication/verification chain succeeds. A green historical implementation run is never treated as publication proof.

## Release evidence

Final release qualification must bind evidence to the exact selected source SHA and follow Linura's normal trusted release process. The release authorization is itself constrained: it must be a single-parent metadata-only commit immediately above the reviewed release-preparation source, retain the identical Git tree, and record that parent/tree as `Reviewed-Source` and `Reviewed-Tree` commit trailers. `tools/release_contract.py validate-intent` is enforced before proof dispatch and again by Trusted Release Proof.

Human prose in this file does not override machine-generated CI, VM, proof, artifact or publication results.
