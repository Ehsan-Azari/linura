# v0.3.0 release-preparation review record

**Decision:** implementation complete; freeze is eligible for protected release qualification, not yet publication.
**Claim class:** Experimental
**Implementation main SHA:** `e7ae91085e200a90867eadd19f9f4dac39f0d769`

## Reviewed scope

The v0.3.0 release preparation freezes the authority-side review milestone implemented by PR #45 and PR #49: canonical reconciliation-plan lineage, Control-owned trusted risk classification and policy orchestration, exact authenticated-principal/evidence/policy/provenance binding, bounded process-local approval semantics, and non-executable Control1/SDK/CLI review/explanation.

## Review findings closed before freeze

The implementation review history found and corrected substantive authority defects rather than waiving them:

- forged or weakened caller-constructed policy evaluation could not remain an approval authority input; issuance now consumes opaque Control-owned trusted review material;
- revocation and current time could not remain caller-selected validation inputs; Control owns authoritative revocation and time;
- wall-clock rollback could not revive expired evidence; authority time is monotonic/fail-closed;
- retained-review bounds must be checked before cloning hostile material;
- expired/revoked live entries are reclaimed into bounded replay tombstones instead of exhausting daemon capacity;
- wire review decoding rejects approval classes weaker than reviewed risk, protected `allow`, `blocked`/allow mismatch, and `change-proposed` with read-only reviewed risk;
- CLI plan-review/explanation preserves semantic reason summary and intent/requirement/capability origins;
- D-Bus authentication remains distinct from human/admin approval and models/agents remain untrusted proposers.

PR #49's final exact head `a68d622ef0ebc9c881f99ff4dd2cac14f6eff159` passed canonical CI, Security, CodeQL, both exact-source VM gates, and a fresh final Codex no-findings review. It was then merged with an expected-head guard as protected-main SHA `e7ae91085e200a90867eadd19f9f4dac39f0d769`; fresh main CI, Security, and CodeQL also passed before release preparation began.

## Release boundary

This freeze does not claim a trusted-human D-Bus approval adapter, durable authorization, durable prepare/commit, crash recovery, Polkit authority, privileged executor integration, public apply/execute, post-effect verification, managed external mutation, supported platform/machine/hardware profile, natural-language/model authority, or production readiness.

A valid approval remains evidence for a review decision only. It is not an execution capability.

## Publication decision rule

Publication remains blocked unless the exact release-preparation source receives its configured review and permanent gates, a separate tree-identical `release: v0.3.0 — policy, authorization, approval, and plan review` authorization is merged onto protected main, that exact release-intent SHA passes fresh CI/Security/CodeQL, Trusted Release Proof reruns both mandatory VM scenarios and reproducibility qualification, promotion consumes the sealed proven bytes without rebuilding, final publication creates the tag last under immutable-release protection, and independent release verification succeeds.
