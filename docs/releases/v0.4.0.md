# v0.4.0 — durable transaction and recovery foundation

**Status:** implementation complete; publication evidence is established only by the protected release lifecycle.
**Claim class:** Experimental
**Supported platform profiles:** none

## Outcome

v0.4.0 proves an Experimental durable reviewed-authority transaction and recovery foundation on top of the v0.3 review/approval boundary, without adding a supported executor or a Linura-managed external machine effect.

The qualified authority path is:

```text
authenticated principal + reviewed plan + current authoritative observation
→ Control-owned prepare revalidation
→ immutable durable transaction/generation binding
→ fresh handoff-time principal/observation/policy/risk/approval revalidation
→ durable Prepared → Indeterminate CAS
→ one-shot process-local non-reconstructible dispatch permit
→ authoritative recovery classification
→ Verified / RecoveryBlocked / explicit no-effect N+1 reprepare
→ verified commit metadata + append-only authenticated audit
```

No v0.4 path invokes an executor. The durable handoff/recovery machinery closes authority, crash, concurrency, integrity, migration, and storage-recovery boundaries before executor qualification begins in v0.5.

## User-visible capability

v0.4.0 adds no supported apply/execute/mutate product surface. Existing Experimental observation, deterministic plan-preview, and plan-review capabilities remain available. The durable transaction/recovery foundation is an internal authority substrate exercised by deterministic tests and repository-owned qualification.

A transaction ID, generation, durable `Prepared`, `Indeterminate`, `Verified`, or committed record is evidence about the authority lifecycle; none is a public mutation credential. A process-local dispatch permit is sealed, non-cloneable, non-serializable, non-persistable, and non-reconstructible after restart, and v0.4 has no executor that can consume it to change Linux.

## Implemented scope

- Persistence-neutral `linura-transaction` domain with typed transaction/generation identities, deterministic domain-separated authority bindings, immutable generation history, idempotency, transition rules, and recovery serialization.
- Control-owned non-cloneable transaction signer; persistence receives verification authority only and cannot mint trusted Control requests or dispatch permits.
- Current authenticated-principal binding plus prepare/handoff/recovery revalidation of authoritative observation, trusted review, policy/risk provenance, and required approval.
- Signer-authenticated validity windows enforced after SQLite write serialization so lock waits cannot extend expired authority.
- Restart-safe handling of durable `Prepared`, sticky `Indeterminate`, durable verified commit material, resume/commit, and explicit no-effect recovery into exactly one next generation.
- SQLite/WAL persistence with `synchronous=FULL`, application/schema identity, immutable migration ledger, exact V1→V2 migration validation, and bounded live-schema validation.
- Separately provisioned `SqliteIntegrityKey` authenticating retained transaction, generation, and audit records; raw writers can alter bytes but cannot mint valid keyed integrity tags.
- Literal CI pins for released migration bodies so an assigned V1/V2 migration identity cannot silently drift with runtime checksum code.
- Bounded persisted-input materialization, aggregate capacity checks, logical audit reservations, and physical recovery reservations.
- Same-filesystem physically allocated recovery reserve with at least 1 MiB per slot, crash-tail reconciliation, regular-file/path/inode/device/link validation, and fail-closed replacement/symlink handling.
- Explicit `open_for_terminal_recovery` and `open_for_terminal_recovery_with_limits` for qualified true-ENOSPC Prepared retirement. Before SQLite opens, only the dedicated opener slot may be physically deallocated through bounded Linux hole punching while logical sidecar length is preserved.
- Terminal-recovery open blocks normal prepare/handoff/recover/commit authority and preserves caller-supplied `StoreLimits`.
- Every audit-reservation `DELETE` is filesystem-neutral while SQLite can still roll back. Physical reserve shrinking occurs only after durable SQLite commit through authenticated, write-serialized reconciliation, including multi-reservation retirement.
- Non-mutating database identity/schema preflight before persistent journal-mode transition, preventing rejected unrelated SQLite databases from being modified.
- Permanent positive/negative/concurrency/fault regressions covering authority substitution, raw-persistence tampering, forged/replaced history, migration compatibility, custom recovery limits, one- and two-reservation rollback, restart/resume, CAS races, stale/mixed evidence, key lifecycle, capacity exhaustion, real ENOSPC recovery, and durability faults.

## Authority and security boundary

Models, agents, transports, and ordinary callers remain untrusted proposers. `linura-control` owns trusted prepare, handoff, recovery, and commit orchestration. Persistence verifies authenticated requests and retained records but does not own policy, approval, principal authentication, or authority minting.

Policy `Allow`, approval evidence, a reviewed plan, transaction/generation identity, durable `Prepared`/`Indeterminate`/`Verified` state, or an audit row does not authorize a Linux mutation. v0.4 adds no public apply/execute/mutate API, generic privileged command surface, Polkit grant, supported executor integration, or Linura-managed external effect.

Raw filesystem/database writers can physically modify SQLite bytes. v0.4 protects retained authority history by keyed record authentication, schema/migration identity, immutable-history checks, and fail-closed validation. It does not claim that coherent rollback of the entire authority database/filesystem/VM snapshot can be detected without a future independently protected monotonic anchor/restore protocol.

## Platform and hardware scope

No Linux distribution, desktop/headless profile, machine class, hardware tier, virtualization profile, or fleet environment is supported by the v0.4.0 product claim.

Repository qualification uses disposable Ubuntu 24.04 LTS amd64 QEMU guests and explicitly exercises loop-mounted ext4 ENOSPC plus SQLite/WAL fault scenarios. Those environments are evidence fixtures, not supported Linura platform profiles.

The durability claim is limited to named local-filesystem/storage assumptions that honor the locking and sync behavior exercised by qualification. `PRAGMA synchronous=FULL` is part of the design but is not presented as a universal hardware/storage durability guarantee.

## Persistence, migration and upgrade

v0.4.0 introduces the first release-qualified local durable authority database for transaction/recovery state. SQLite carries explicit application/schema identity, repository-visible migration identity, immutable migration-ledger verification, bounded schema validation, and fail-closed handling of corruption, unsupported newer schema, and migration mismatch.

Released schema V1 remains immutable. Existing canonical schema-v1 stores are authenticated against the historical V1 schema fingerprint, exact one-entry migration ledger, and authority-store identity before the additive V2 migration is applied. V2 installs terminal-recovery trigger semantics, records its own ledger checksum once, and advances `user_version` to 2. Fresh stores apply V1 then V2. CI independently pins the literal released V1/V2 bodies.

Durable idempotency is scoped to one monotonic authority-database history. Exact retained generations are immutable and one current-generation/state-version serialization point governs mutually exclusive recovery outcomes. The store maintains bounded authenticated audit history and explicit logical/physical recovery reservations rather than silently evicting authority history under capacity pressure.

An unrelated or unidentified existing SQLite database is rejected before Linura makes a persistent journal-mode change. A genuinely empty unidentified database can be initialized transactionally and restartably.

## Recovery and rollback

`Indeterminate` is sticky across restart and never becomes retry authority merely because a process restarted or a permit was lost. Recovery requires fresh authoritative re-observation and current authority revalidation.

Authoritative intended-state evidence may advance only the exact current indeterminate generation to `Verified`; conflicting evidence may advance only that exact generation to `RecoveryBlocked`; ambiguous or stale evidence leaves it indeterminate. Authoritative no-effect evidence can permit explicit current-authority re-establishment and exactly one N+1 `Prepared` generation while retiring N atomically; it does not itself grant retry authority.

For a qualified Prepared terminal exit at genuine filesystem ENOSPC, the sidecar retains a dedicated opener slot. A special recovery open validates the database/sidecar path and SQLite page size before SQLite open, punches only that opener slot while keeping the sidecar's logical layout stable, then opens and validates the authority database. The special mode cannot silently become generic mutation authority.

Reservation deletion itself never shrinks filesystem reserve while the SQLite transaction can roll back. This invariant applies at every reservation cardinality. After a successful durable transition, authenticated post-commit reconciliation shrinks only provably excess physical allocation. Therefore a process death or failed SQLite commit before durability leaves the full rollback-required reserve shape intact, while a crash after commit but before cleanup leaves only excess allocation that a later open may safely shrink.

Real loop-mounted ext4 qualification proves Prepared retirement after true ENOSPC, new-process terminal-recovery open, durable abort, reopen, unmount/remount, and terminal integrity validation. Separate regressions prove both one-reservation and two-reservation DELETE/ROLLBACK windows retain deterministic recovery capacity.

Coherent whole-database/filesystem/VM-snapshot rollback remains unsupported and is not claimed detectable in v0.4 without an external monotonic anchor/restore protocol.

## Compatibility boundary

Product version `v0.4.0`, SQLite schema version, migration IDs, D-Bus generation `Control1`, and checked `*.v1` schemas are independent version axes. The v0.4 durable authority contracts remain Experimental and do not become Stable merely because they are public, persisted, or included in a release.

Released migration identities are immutable: V1 remains byte-pinned and V2 is additive. Existing supported v0.4 authority databases must satisfy application identity, exact migration ledger/checksums, schema fingerprint, verifier identity, integrity-key identity, and configured storage bounds before migration or normal use proceeds.

No compatibility shim revives superseded executable/provider-owned planning paths, and no v0.4 durable record is promoted into a generic executor credential.

## Required acceptance evidence

The final implementation repair is [PR #62](https://github.com/linura-org/linura/pull/62). Exact head [`8ebf8e3c3b861f222c228c379ea21478e296b205`](https://github.com/linura-org/linura/commit/8ebf8e3c3b861f222c228c379ea21478e296b205) passed the complete repository-controlled qualification set before guarded merge:

- CI run `33917843348`;
- Security run `33917843428`;
- CodeQL run `33917843396`;
- Disposable VM acceptance run `33917843410`;
- Control1 plan-preview VM acceptance run `33917843590`;
- genuine ext4 ENOSPC recovery run `33917843426`;
- SQLite/WAL durability fault run `33917843352`.

It was guarded squash-merged to protected main as [`072ca08c92a8e146aab49ded8a7f94bf646394d6`](https://github.com/linura-org/linura/commit/072ca08c92a8e146aab49ded8a7f94bf646394d6).

The durability suite includes deterministic write denial, SIGKILL after acknowledged durable handoff, abrupt QEMU power loss, same-image restart, sticky `Indeterminate`, WAL checkpoint, and reopen verification. All actionable implementation review findings were resolved after qualification, including immutable V1→V2 migration, caller-supplied recovery limits, commit-ordered reserve shrink for one and multiple reservations, and literal migration-body anti-drift pins.

Before publication, the exact release-refresh source must additionally pass applicable CI, Security, CodeQL, configured review, release-contract validation, and have no unresolved blocking review thread. The subsequent tree-identical release authorization must pass fresh protected-main checks and Trusted Release Proof must rerun authoritative-observation VM, Control1 plan-preview VM, v0.4 SQLite/WAL durability fault, and genuine loop-mounted ext4 ENOSPC recovery before build/promotion.

## Known limitations and unsupported states

- No supported executor or independent post-effect verifier exists in v0.4.
- No public apply/execute/mutate API or supported Linura-managed external effect exists.
- No trusted platform profile, Linux distribution, desktop/headless target, hardware tier, or virtualization/fleet environment is supported by the v0.4 claim.
- Terminal ENOSPC recovery qualification is scoped to named local Linux/ext4 assumptions and Linux hole-punch support.
- Coherent rollback of the complete authority database/filesystem/VM snapshot is unsupported without an independently protected monotonic anchor/restore protocol.
- SQLite `synchronous=FULL` does not by itself establish universal durability for arbitrary filesystems, hardware, controllers, or hypervisors.
- Control1 and the durable authority contracts remain Experimental and may evolve coherently before any Stable promotion.

## Explicit non-goals

v0.4.0 deliberately does **not** claim:

- production readiness or a supported installable Linura operating system;
- an executor or independent post-effect verifier;
- Polkit authority, privileged credential acquisition, or generic shell execution;
- a public apply/execute/mutate API;
- supported package/service/network/storage/desktop mutation;
- any supported Linura-managed external effect;
- complete eleven-stage managed mutation;
- agent/model execution authority or durable agent authority;
- a supported Linux distribution, machine class, hardware, virtualization, or fleet profile;
- transparent coherent database/filesystem/VM-snapshot rollback recovery;
- universal durability from SQLite `synchronous=FULL` alone.

No supported managed external effect exists in v0.4. v0.5 may qualify a narrow executor/verifier under disposable qualification authority only; v0.6 remains the first milestone permitted to publish a bounded supported Experimental managed external effect.

## Traceability

- [PR #53](https://github.com/linura-org/linura/pull/53) — v0.4 milestone/architecture contract.
- [PR #54](https://github.com/linura-org/linura/pull/54) — durable authority implementation; final head [`e92786f2c4e545bd289692f6199060da9e3a0248`](https://github.com/linura-org/linura/commit/e92786f2c4e545bd289692f6199060da9e3a0248); protected-main merge [`5e9e9bff03dcd24bca0f3bb3c735e37b49aacdd7`](https://github.com/linura-org/linura/commit/5e9e9bff03dcd24bca0f3bb3c735e37b49aacdd7).
- [PR #58](https://github.com/linura-org/linura/pull/58) — rollback/migration-ledger hardening; final head [`1a3a9b343d3830f384128fe6e42a2a0c2a394898`](https://github.com/linura-org/linura/commit/1a3a9b343d3830f384128fe6e42a2a0c2a394898); protected-main merge [`4f6ad21aa45cbce783bdd6c6ef7e3cc347dca18b`](https://github.com/linura-org/linura/commit/4f6ad21aa45cbce783bdd6c6ef7e3cc347dca18b).
- [PR #61](https://github.com/linura-org/linura/pull/61) — physical reserve and release-proof hardening; final head [`ee1e82608f9693f47eb72ea88cf641154e462232`](https://github.com/linura-org/linura/commit/ee1e82608f9693f47eb72ea88cf641154e462232); protected-main merge [`c92c1e161eb4b60b39170c3ae24253ae81c453c8`](https://github.com/linura-org/linura/commit/c92c1e161eb4b60b39170c3ae24253ae81c453c8).
- [PR #62](https://github.com/linura-org/linura/pull/62) — final pre-open ENOSPC, V1→V2, custom-limit, and all-cardinality commit-order hardening; final exact head [`8ebf8e3c3b861f222c228c379ea21478e296b205`](https://github.com/linura-org/linura/commit/8ebf8e3c3b861f222c228c379ea21478e296b205); protected-main merge [`072ca08c92a8e146aab49ded8a7f94bf646394d6`](https://github.com/linura-org/linura/commit/072ca08c92a8e146aab49ded8a7f94bf646394d6).
- Claim/evidence dossier: `docs/qualification/v0.4.0.md`.
- Release-review record: `docs/qualification/v0.4.0-release-review.md`.

## Artifacts and supply-chain evidence

Trusted Release Proof builds the exact selected protected-main release source into the repository's sealed release payload and independently reproduces required distributable binaries before promotion. The payload must include source/version identity, frozen release notes, checksums, SPDX SBOM, release evidence, build-environment data, and repository-defined provenance/attestation.

Artifact presence does not expand the v0.4 product claim. Workspace executor/bootstrap/update binaries remain scaffolding or future-qualification components unless independently promoted by a later milestone; they are not evidence of supported mutation or installation in v0.4.

## Publication evidence

Publication evidence exists only after a fresh release-refresh source has passed its applicable checks and documented adversarial review, then a single-parent metadata-only `release:` authorization is created immediately above that reviewed source with an identical Git tree and exact `Reviewed-Source: <parent-sha>` / `Reviewed-Tree: <tree-sha>` trailers.

`python3 tools/release_contract.py validate-intent --source-sha <authorization-sha>` must validate that authorization before proof dispatch and Trusted Release Proof must independently revalidate it. The authorization then requires fresh protected-main CI/Security/CodeQL, mandatory observation/Control1/durability/ENOSPC qualifications, isolated exact-source build, independent reproduction, sealed evidence creation, promotion of the exact proven bytes without rebuild, proof-first/tag-last immutable publication, and independent fresh-runner Release Verification of tag/source identity, release body, checksums, evidence, assets, immutability, and repository-defined attestations.

A merged implementation PR, green release-refresh PR, metadata-only authorization commit, or successful proof job alone is not proof that v0.4.0 is released.

## Next-version handoff

Only after independent Release Verification succeeds may roadmap bookkeeping advance `current_release` to `v0.4.0` and `next_release` to `v0.5.0`, mark the v0.4 milestone released, update README release status, and remove obsolete working/qualification branches.

The v0.4 durable transaction, generation, audit, recovery, and dispatch-permit semantics remain inputs to future execution qualification rather than standalone execution credentials. v0.5 remains executor/verifier qualification-only, and v0.6 remains the first milestone permitted to publish a bounded supported Experimental managed external effect.
